{
  "buildCommand": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt",
  "outputDirectory": ".",
  "devCommand": "source .venv/bin/activate && export FLASK_APP=wsgi.py && export FLASK_ENV=development && python3 -m flask run --host=0.0.0.0 --port=$PORT",
  "installCommand": "source .venv/bin/activate && pip install -r requirements.txt",
  "framework": null,
  "env": {
    "FLASK_APP": "wsgi.py",
    "FLASK_ENV": "production"
  }
}
