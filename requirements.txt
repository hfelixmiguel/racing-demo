# Core Game Engine
arcade>=2.7.0 --no-deps
numpy>=1.24.0 --no-deps

# Testing
pytest>=7.4.0 --no-deps

# Packaging (local development only)
pyinstaller>=6.0.0 --no-deps

# Vercel Deployment - Use pip flags for externally-managed environments
flask>=3.0.0
gunicorn>=21.0.0
